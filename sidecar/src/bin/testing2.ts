export class A {
    // rest of the code ..

    secondFunction(a: number, b: number): number {
        return a * b;
    }
}

function add(a: number, b: number) {
    if (a > 0) {
        return a + b;
    }
}
